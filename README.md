# Inlämmning Uppgift 1

## Description:

A simple node express application that shows information retrieved from your self-built API and from an optional external API.

## Requirements:

Node.js NPM Express

After creating the App folder in terminal following commonds were used:

npm i express
node server.js
npm install -D nodemon
